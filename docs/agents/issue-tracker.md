# Issue Tracker

Issues are tracked as local Markdown files under `.scratch/<feature>/`.
Use this repository-local workflow instead of `gh` or `glab`.

Pull requests are not an issue intake surface for this repository.
